import '../dimensions/dimensions.dart';

class StringConstants {
  static const String appName = 'taskManager';
  static const String taskManagerFontFamily = 'SpaceGrotesk';
  static final SizeConfig config = SizeConfig();
}
